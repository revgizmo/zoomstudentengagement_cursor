% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/participant_classification.R
\name{classify_participants}
\alias{classify_participants}
\title{Classify Participants (Pure, Privacy-Aware)}
\usage{
classify_participants(
  transcript_df,
  roster_df,
  lookup_df = NULL,
  privacy_level = getOption("zoomstudentengagement.privacy_level", "mask")
)
}
\arguments{
\item{transcript_df}{Data frame of transcript utterances.
Must contain one of: \code{transcript_name}, \code{name}, \code{speaker_name}, \code{participant_name}.}

\item{roster_df}{Data frame of enrolled students.
Should contain one of: \code{first_last}, \code{preferred_name}, \code{formal_name}, \code{name}, \code{student_name},
and optionally \code{student_id}.}

\item{lookup_df}{Optional data frame of name mappings as produced by
\code{read_lookup_safely()}.}

\item{privacy_level}{One of \code{c("ferpa_strict", "ferpa_standard", "mask", "none")}.
Defaults to \code{getOption("zoomstudentengagement.privacy_level", "mask")}.}
}
\value{
The \code{transcript_df} augmented with columns:
\code{clean_name}, \code{participant_type}, \code{student_id}, \code{is_matched}.
}
\description{
Given transcript utterances, a student roster, and an optional lookup,
returns a data frame with classification columns that identify each
utterance's \code{clean_name}, \code{participant_type}, \code{student_id}, and
\code{is_matched}. This function is pure and performs no filesystem writes.
}
\details{
Privacy defaults are applied to outputs. Use this to classify participants
BEFORE computing metrics so that downstream functions operate on a
privacy-safe, idempotent representation.
}
