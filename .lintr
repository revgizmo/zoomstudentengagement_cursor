linters: linters_with_defaults(
  # Keep line_length_linter disabled to avoid noisy diffs in roxygen and vignettes
  line_length_linter = NULL,
  # Keep object_length_linter relaxed; package exports can be verbose
  object_length_linter = NULL
) 
